# 入局·三国篇 — 项目接手指令（v2 · 适配现有 history-app 项目）

> **本文档面向 Claude Code（项目里的开发 AI）。**
> 用户没有编程经验，他依赖你来执行所有技术工作。请严格按照本文档执行。
>
> 完成第一、二步之前**不要写任何代码**、**不要修改任何现有文件**。

---

## 一、必读文档（按顺序）

按以下顺序读完，**不要跳读**:

1. **`codex_handoff.md`**（项目根目录）—— 之前 Codex 留下的完整开发历史
2. **`docs/PROJECT_BIBLE.md`**（用户已上传）—— 新故事（三国篇）的完整设计圣经
3. **本文档（SETUP_GUIDE.md）**—— 把上面两份文档桥接到具体行动

读完后你应该能回答（自检即可，无需告诉用户）：

- history-app 项目当前有哪四个模块？
- "入局"模块经历过哪几代方向？当前是什么？
- 公开课"礼法断案"为什么必须隔离？
- 阿萤的两个秘密是什么？
- 三国篇的写作风格和"驿路无名"为什么必须不同？

如果不能回答任一问题，重读。

---

## 二、项目全貌（30 秒概括）

**history-app**（时空印证系统）是一个综合性历史互动平台，包含四个模块：

| 模块 | 当前状态 | 本次改动 |
|---|---|---|
| 📚 历史长卷 | 80+ 历史事件卷宗 | **不动** |
| 💬 历史人物 AI 对话 | 已实现，对接百炼 | **不动**（但可参考其 prompt 设计） |
| 🎮 互动游戏 | "猜历史人物"试玩版 | **不动** |
| 🎭 **入局** | 当前是"驿路无名"（唐） | **彻底重做，改为三国篇** |

**附加模块**：公开课"礼法断案"（5 个法律案例）—— **必须保持完全隔离，绝不能动**。这是 2026-06-11 已经在真实课堂上用过的功能。

---

## 三、关于"文档"与"代码"的关系（极其重要，必读）

策划 Claude（写本文档和 PROJECT_BIBLE 的那位）**只能读到 codex_handoff.md 描述的项目状态**——它没有完整的代码视野。**你有**。

这意味着本文档不可避免地会有：
- **过时**的信息（codex_handoff 最后更新是 2026-06-18，之后可能项目又变了）
- **不完整**的描述（文档不可能覆盖所有代码细节）
- **不准确**的判断（codex 自己也可能有理解偏差）

### 核心原则

**代码是真相之源**。本文档是路线图，不是判决书。

具体来说：

1. **代码 > 文档**：本文档或 PROJECT_BIBLE 中提到的项目状态与你实际看到的代码不符时，**以代码为准**。

2. **主动汇报偏差**：发现文档过时、不准确、或者不完整的地方，**主动告诉用户**，并提出你基于真实代码的修正建议。

3. **自主优化决策**：本文档给的目录结构、接口路径、文件清单、技术方案等**只是建议**。如果你看完真实代码发现有更合理的做法——比如：
   - 现有项目已有某种成熟模式可以复用
   - 某个文件已经被重构了不存在
   - 某个接口路径会与现有冲突
   - 我建议的目录结构和现有项目惯例不符
   - 我推荐的技术栈和现有已用的库重复或冲突
   - 我列的"删除清单"实际上比真实情况少/多
   
   **请采用你的方案**，并告诉用户为什么。你不需要因为文档这么写就照搬。

4. **HOW 你决定，WHAT/WHAT NOT 文档决定**：
   - **怎么做**（HOW）：技术路径、目录命名、接口设计、代码组织——你来决定，基于真实代码
   - **做什么 / 不做什么**（WHAT / WHAT NOT）：以下原则**永远以文档为准**，无论代码现状如何：
     - 严格正史，不用《三国演义》虚构情节
     - 公开课"礼法断案"绝对隔离，不能动
     - 历史长卷、人物 AI 对话、互动游戏不动
     - 阿萤人设、写作风格、UI 哲学按 PROJECT_BIBLE 执行
     - 删除"驿路无名"全部痕迹

5. **汇报你的判断，不只是执行结果**：每个阶段汇报时，告诉用户：
   - 我看到代码后发现了哪些与文档不符的地方
   - 我做了什么调整、为什么
   - 哪些是我建议改而你（用户）需要确认的

### 当你犹豫时

- 不确定该听文档还是该相信代码 → **相信代码，但告诉用户**
- 不确定文档建议的方案是否合理 → **采用你的方案，但告诉用户为什么**
- 不确定核心原则是否适用某个特殊情况 → **问用户**

策划 Claude 不会因为你修改了它的建议而"不开心"——它知道你看得见它看不见的东西，这是好事。

---

## 四、用户的关键决策（不可商量）

这三条是用户在与策划 Claude 反复讨论后做出的明确决策。**这些是命令，不是建议**。

### 决策 1：彻底删除"驿路无名"

- 用户已确认：**不保留**，**不归档**，**彻底删除**
- 这意味着删除：
  - `static/js/historical_rpg_demo.js`
  - `static/css/historical-rpg-demo.css`
  - `static/images/ruju-demo/` 整个文件夹
  - `static/audio/ruju-demo/` 整个文件夹（包括所有 wav、json 元数据）
  - `data/intrigue_scenes_*.json` 中所有非公开课的旧场景数据
  - `static/index.html` 中所有 `historical_rpg_demo` 引用
  - `static/js/app.js` 中所有 `historical_rpg_demo` 相关接入逻辑
- **但必须确认**：删除前先 grep 整个项目，确保没有遗漏引用导致功能崩溃

### 决策 2：UI 完全重做

- **从零重做**，按 `docs/PROJECT_BIBLE.md` 第七节的 UI 想象
- 现有的字幕式对白、立绘、地图等代码**只作参考，不直接复用**
- 核心 UI 哲学：**"电影 × 古籍"的混合体**，深色 + 暖灯色，宋体/楷书，仿古印章式选择
- 详见 PROJECT_BIBLE 第七节

### 决策 3：继续使用阿里百炼

- 模型：`qwen3.6-flash`（已配置）或更高级的 qwen 模型
- API key 已在 `.env` 中（`DASHSCOPE_API_KEY`）
- 现有 `/classroom/talk_stream` 等接口的流式输出能力**可以复用**到三国篇
- **但需要新写适合三国篇的 prompt 模板**——参考 PROJECT_BIBLE 第六节的"状态卡"和"知识边界卡"

---

## 五、可以做的事 vs 不能做的事

### ✅ 可以做（在用户确认后）

**入局模块相关**：
- 删除"驿路无名"全部代码和资源
- 重构"入局"模块为多故事支持的架构（虽然第一版只有三国篇）
- 创建三国篇前端代码（HTML/CSS/JS）
- 创建三国篇数据文件（JSON 剧本数据）
- 修改 `api.py` 中**仅与入局相关**的接口
- 在 `static/`、`data/`、`docs/` 下新建三国篇相关子目录

**文档相关**：
- 创建 `docs/` 文件夹及其子文件
- 创建 `CLAUDE.md` 入口文件
- 拆分 PROJECT_BIBLE 到独立子文档（按下面第五节）

### ⛔ 绝对不能做

1. **不能动公开课**：
   - `/classroom/*` 接口
   - `static/classroom_admin.html`
   - `data/intrigue_scenes_law_classroom.json`
   - 任何带 `classroom`、`law_classroom`、`lawvisual` 的代码

2. **不能动其他三个模块**：
   - 历史长卷（`events.yaml` 及相关）
   - 历史人物 AI 对话
   - 互动游戏（猜历史人物）

3. **不能动用户系统和基础设施**：
   - 登录、统计、限流、错误兜底
   - `auth_data/`、`analytics_data/`、`chat_data/` 文件夹
   - `.env` 等环境配置

4. **不能动旧的 career 系统**（虽然已废弃但用户未明确删除）：
   - `career_engine.py`
   - `data/career_*.json`
   - 这些是上一代官场原型的遗留，先**保留**，后续可能用作官制资料参考

5. **不能用《三国演义》虚构情节作为正史背景**（详见 PROJECT_BIBLE 第三节）

6. **不能让历史人物预言未来**

### ⚠️ 需要警惕的"残留代码"

根据 codex_handoff.md，已知问题：

- `static/js/app.js` 的 `handleTravelChoice()` 中存在永远不会执行的重复代码
- 正式入局入口曾被硬编码为公开课"礼法断案"文案——重做时必须区分
- `_build_intrigue_payload()` 中残留赤壁专属的"行动力耗尽"硬编码文案
- 删除"驿路无名"时，必须 grep 所有引用，避免删一处坏一处

---

## 六、实施顺序（五个阶段）

**每个阶段完成后向用户汇报、等用户确认，再进入下一阶段。不要自作主张连续推进。**

### 阶段一：文档结构（无代码风险，先做这个）

目标：让所有文档就位，建立稳定的协作基础。

> 💡 **自主调整空间**：本节给的目录结构和文件命名是建议。如果你看到现有项目已有 `docs/`、`documentation/` 等约定，或者其他文件命名风格，请遵循现有惯例，不要为了照搬本文档而破坏一致性。

**步骤**：

1. **创建目录**：
   ```
   docs/
   docs/chapters/
   ```

2. **拆分 PROJECT_BIBLE.md 为独立子文档**（保留主文档不动）：

   | 新文件 | 内容来源 |
   |---|---|
   | `docs/01_STORY_BIBLE.md` | BIBLE 第 1-4 节（项目简介、参考、正史原则、故事规划） |
   | `docs/02_CHARACTERS.md` | BIBLE 第 5 节（人物设定） |
   | `docs/03_AI_SYSTEM.md` | BIBLE 第 6 节（AI 三层架构） |
   | `docs/04_UI_SPEC.md` | BIBLE 第 7 节（UI 想象） |
   | `docs/05_WRITING_STYLE.md` | BIBLE 第 8 节（写作风格规范） |
   | `docs/06_PROMPT_TEMPLATES.md` | BIBLE 第 6.2、6.3 节模板示例集中整理 |
   | `docs/chapters/00_prologue.md` | BIBLE 第 9.1 节（序章·涿郡） |
   | `docs/chapters/01_interlude.md` | 占位符（五年间过渡，待写） |
   | `docs/chapters/02_luoyang.md` | BIBLE 第 9.3 节（第一站·洛阳） |
   | `docs/chapters/03_changan.md` 至 `07_chibi.md` | 各占位符 |

   **每个拆分文件顶部添加**：
   ```markdown
   > 本文档抽取自 `PROJECT_BIBLE.md` 第 X 节
   > 主文档保留为源材料，所有变更需同步至主文档
   ```

3. **创建 `CLAUDE.md`**（项目根目录），内容见本文档附录 A

4. **完成后**：
   - 列出你创建/修改的所有文件
   - 简短总结你对项目的理解（5-8 句话）
   - 等用户确认进入阶段二

### 阶段二：归档与清理（有代码风险，谨慎进行）

目标：清除"驿路无名"全部痕迹，但不破坏其他功能。

> 💡 **自主调整空间**：本节给的"删除清单"是基于 codex_handoff.md 的盘点，**可能不完整或已过时**。请用 grep/find 实际扫描代码，列出**你看到的真实删除清单**——可能比文档列的多（漏掉的相关文件）或少（已经被删了）。汇报时把差异告诉用户，让他确认。

**步骤**：

1. **先用 grep 全项目扫描**，列出所有引用：
   ```bash
   grep -rn "historical_rpg_demo" --include="*.js" --include="*.html" --include="*.py" --include="*.css"
   grep -rn "ruju-demo" --include="*.js" --include="*.html" --include="*.py" --include="*.css"
   grep -rn "yi_lu_wu_ming\|驿路无名" --include="*.js" --include="*.html" --include="*.py" --include="*.json"
   ```

2. **生成"将删除文件清单"**，向用户汇报，**等用户确认后**才动手

3. **删除前先 commit 当前状态到 git**（让用户可以回退）

4. **逐个删除**，每删一组验证项目仍能启动：
   - 第一组：媒体资源（`static/images/ruju-demo/`、`static/audio/ruju-demo/`）
   - 第二组：前端代码（`static/js/historical_rpg_demo.js`、`static/css/historical-rpg-demo.css`）
   - 第三组：HTML 引用（修改 `static/index.html`）
   - 第四组：app.js 接入逻辑
   - 第五组：api.py 中的"驿路无名"相关 payload 处理

5. **每组删除后**：
   - 启动本地服务（`python api.py` 或项目配置的启动命令）
   - 访问主页、长卷、人物对话、游戏、公开课各一次，确认没坏
   - 确认公开课五个案例仍能正常进入

6. **完成后**：汇报清理结果，等用户确认进入阶段三

### 阶段三：入局模块架构重构

目标：把"入局"从单故事改为支持多故事的架构（虽然第一版只有三国篇）。

> 💡 **自主调整空间**：本节给的目录结构、接口路径、JSON 文件设计**都只是建议**。请评估 `api.py`、`app.py`、`static/js/app.js` 的真实架构和惯例，给出更适配现有项目的方案。例如：
> - 如果现有接口都用 `/time_travel/...` 前缀，沿用而非新创 `/intrigue/...`
> - 如果现有数据都在 `data/` 直接放 JSON 而非分子目录，遵循现有风格
> - 如果现有 prompt 都集中在 `api.py` 里某个函数里，先评估是否需要拆出来再决定
> 
> **重点**：你的方案如果和文档不同，**汇报时说清楚为什么你的方案更好**，让用户决定。

**步骤**：

1. **设计数据结构**：
   ```
   data/stories/
   ├── README.md                         ← 多故事架构说明
   └── sanguo/                           ← 三国篇
       ├── manifest.json                 ← 故事元信息（标题、朝代、章节列表）
       ├── prologue.json                 ← 序章剧本数据
       ├── characters.json               ← 人物设定（阿萤、主角、历史人物）
       ├── prompts/                      ← AI prompt 模板
       │   ├── ayinghuo_template.json    ← 阿萤状态卡模板
       │   └── historical_*.json         ← 历史人物知识边界卡
       └── chapters/
           └── 02_luoyang.json
   ```

2. **设计入局入口的"故事选择器"**——即使现在只有三国篇，也要预留多故事的入口逻辑

3. **改造 api.py 入局相关接口**为通用接口：
   - `/intrigue/stories` 列出可用故事
   - `/intrigue/story/<story_id>/load` 加载特定故事
   - `/intrigue/story/<story_id>/scene/<scene_id>` 加载特定场景
   - `/intrigue/story/<story_id>/companion_talk_stream` 阿萤自由对话（流式）
   - `/intrigue/story/<story_id>/historical_talk_stream` 历史人物受限对话（流式）

4. **完成后**：汇报架构，等用户确认进入阶段四

### 阶段四：UI 实现（按 `docs/04_UI_SPEC.md` 从零做）

目标：实现 PROJECT_BIBLE 第七节的 UI 想象。

> 💡 **自主调整空间**：UI 的"哲学和气质"以 PROJECT_BIBLE 为准（电影 × 古籍、深色 + 暖灯色、字幕式对白、印章式选择）。但**具体实现细节**——CSS 类名、组件拆分、动画库、布局方案——由你根据现有项目惯例决定。如果你觉得 PROJECT_BIBLE 某个 UI 想象的细节在技术上不合理或体验不佳，**先告诉用户你的顾虑和替代方案**，让他确认。

**关键交付物**：
- 入局主屏（全屏画面 + 字幕式文字 + 印章式选择）
- 笔记本（"卷"）界面
- AI 自由对话界面（"言"按钮 + 仿古纸输入框）
- 历史人物对话界面（"史"印章 + 轮数提示）
- 章节切换（地图过场动画）

**先实现序章·涿郡**作为最小可玩版本（MVP），跑通后再扩展。

**完成后**：让用户实际体验序章，收集反馈，等用户确认进入阶段五。

### 阶段五：AI 对话系统接入

目标：让阿萤的自由对话和荀彧的受限对话真正可用。

> 💡 **自主调整空间**：PROJECT_BIBLE 第六节给的"三道检查"机制是策划层面的设计思路。具体技术实现——是用 LLM 后置检查、还是用规则系统、还是用 prompt 工程让模型自我约束——由你决定。重点是**达到效果**：历史人物不会预言未来、不会违反人设、不会暴露未到时间的秘密。

**关键工作**：

1. **写 qwen 模型的 prompt 模板**——参考 PROJECT_BIBLE 第六节，但需要根据 qwen 的特性调整
2. **实现"状态卡"加载机制**：每个对话节点加载对应状态卡注入 system prompt
3. **实现"知识边界卡"约束检查**：
   - 生成回复后过三道检查（预言未来 / 违反人设 / 知识越界）
   - 不通过则重试，最多 3 次
   - 仍不通过则触发预设回避回答
4. **实现对话轮数控制**：历史人物对话满轮数后自动结束并推进剧情
5. **实现玩家选择记忆**：把玩家关键选择作为上下文传给阿萤的对话模型

---

## 七、接手时第一步该做什么

读完本文档后，**立刻执行以下三步**：

1. **读 `codex_handoff.md`**（如果还没读）
2. **读 `docs/PROJECT_BIBLE.md`**（如果还没读）
3. **执行项目结构扫描**：
   ```bash
   ls -la
   ls static/js/ static/css/ data/ 2>/dev/null
   git log --oneline -10
   git status --short
   ```

然后**简短汇报**（不超过 15 行），告诉用户：

- 我已读完三份核心文档（codex_handoff、PROJECT_BIBLE、SETUP_GUIDE）
- 我对项目当前状态的理解是：[3-5 句话]
- **我看到代码后发现与文档描述的偏差**：[列出，如果有的话——没有也明说"代码与文档一致"]
- 我建议从"阶段一：文档结构"开始，因为它无代码风险
- 请确认是否同意从这里开始

**不要主动开始任何代码改动。等用户回应。**

---

## 八、与用户的沟通原则

1. **用普通话解释技术**——不堆术语：
   - ❌ "需要在 api.py 里加一个 endpoint 处理 SSE streaming"
   - ✅ "需要在后端加一个新接口，让 AI 的回复一个字一个字地显示出来，像打字效果。我会处理代码细节。"

2. **每个里程碑后让用户测试**——不要默默推进

3. **报告进度时给具体看得见的东西**（截图、链接、可点击的 demo）

4. **遇到不确定时问用户，不要自由发挥**

5. **遵循 codex_handoff.md 的"外延检查"原则**：
   - 完成了什么
   - 顺手检查了哪些可能被牵动的点
   - 发现了哪些潜在问题
   - 哪些已修，哪些建议用户确认再改

6. **基于代码的判断 > 基于文档的建议**：
   - 当你看到真实代码后发现文档过时或建议不合适，**主动指出**，给出你的替代方案
   - 不要因为"文档这么写"就照搬——文档是路线图，不是判决书
   - 但每次"偏离文档"都要告诉用户：我为什么偏离、我的方案是什么、风险在哪

---

## 九、技术债与已知坑

从 codex_handoff.md 抽取的需要注意的事：

1. **编码问题**：不要用 PowerShell `Set-Content` 直接重写包含中文的前端文件。会编码污染。改中文优先用 `apply_patch` 或直接编辑器。

2. **缓存问题**：修改 CSS/JS 后要更新 `static/index.html` 中的缓存版本号，否则用户浏览器看不到更新。

3. **`api.py` 和 `static/js/app.js` 体积已经很大**——新模块不要堆进这两个大文件，按入局/UI/AI 三大块拆分模块。

4. **百炼模型注意事项**：
   - 当前接口路径：参考 `/classroom/talk_stream` 的实现
   - 模型环境变量：`TIME_TRAVEL_FAST_MODEL`，默认 `qwen3.6-flash`
   - 中文 prompt 在 PowerShell 直接测试可能因终端编码导致问号，验证时用浏览器
   - 优先级：`WEB_API_KEY > DASHSCOPE_API_KEY > GEMINI_API_KEY`

5. **配音不是核心**——三国篇第一版**不做配音**。codex_handoff 反复提到"配音不是玩法核心，AI 语音破坏沉浸感"。先做好文本和 BGM。

6. **BGM 优先级高于配音**——序章·涿郡需要一段冬日萧瑟的氛围 BGM。

---

## 附录 A：CLAUDE.md 入口文件模板

**直接复制以下内容创建项目根目录的 `CLAUDE.md`**：

```markdown
# history-app · 入局·三国篇

这是「时空印证系统」（history-app）的开发项目说明。

## 📂 关键文档（每次开始工作前读）

1. **`codex_handoff.md`** —— 项目历史与当前状态（之前 Codex 的开发记录）
2. **`docs/PROJECT_BIBLE.md`** —— 三国篇的完整设计圣经
3. **`SETUP_GUIDE.md`** —— 项目接手指令（首次接手时读）

## 🎯 当前任务

把「入局」模块从"驿路无名"（唐天宝末年）替换为"三国篇"（东汉末年），并重构为支持多故事的架构。

## ⛔ 绝对不能动

- **公开课"礼法断案"**：`/classroom/*` 接口、`classroom_admin.html`、`data/intrigue_scenes_law_classroom.json`
- **其他模块**：历史长卷、历史人物 AI 对话、互动游戏
- **基础设施**：登录、统计、用户数据

## ✅ 正在做

按 SETUP_GUIDE.md 的五阶段实施顺序推进：
- 阶段一：文档结构（无代码风险）
- 阶段二：清理"驿路无名"
- 阶段三：入局模块架构重构（多故事支持）
- 阶段四：UI 实现（按 docs/04_UI_SPEC.md 从零做）
- 阶段五：AI 对话系统（阿萤 + 历史人物，对接百炼）

## 📐 核心创作原则

详见 `docs/05_WRITING_STYLE.md` 和 `docs/01_STORY_BIBLE.md`，要点：

- **严格正史**：严禁使用《三国演义》虚构情节作为正史背景
- **小人物视角**：玩家不能改写历史，只能改变身边人命运
- **情感先于名词**：不要堆专有名词
- **2-3 句一屏**：节奏必须慢
- **每屏文字越短越好**：能 30 字说清绝不 60 字

## 👤 项目主理人

- 没有编程经验，依赖你完成所有技术工作
- 对故事品质要求极高
- 被《饿殍》《燕云十六声》《盛世天下女帝篇》深度熏陶，审美在线
- 沟通原则：给方案而不是给问题；重要决策给 3—4 个选项 + 利弊
- 期望你执行"外延检查"：完成 A 时主动检查 B、C、D 是否受影响

## 🤝 协作分工

主理人 ←→ 策划 Claude（网页版）→ 产出文档 → 主理人转交 → Claude Code（你）→ 实现代码 → 主理人测试 → 反馈给策划 Claude

**当你与本项目文档冲突时，文档优先，并提醒用户确认。**
```

---

## 附录 B：基础设施盘点（可复用）

以下基础设施已在 history-app 中实现，三国篇可直接复用：

| 能力 | 当前实现位置 | 复用方式 |
|---|---|---|
| 流式 AI 对话 | `/classroom/talk_stream` 等接口 | 仿照实现 `/intrigue/.../talk_stream` |
| 人物边界系统 | 历史人物 AI 对话模块 | 借鉴 prompt 设计，移植到入局 |
| 用户登录 | 现有 auth 系统 | 直接调用 |
| 统计 | `analytics_data/` | 直接调用 |
| 限流 | 现有限流中间件 | 直接调用 |
| 流式前端渲染 | `historical_rpg_demo.js` 的字幕实现 | 删除前**先抄出流式逻辑核心代码作参考** |

---

## 附录 C：文件操作清单

### 阶段二将删除的文件

**前端代码**：
- `static/js/historical_rpg_demo.js`
- `static/css/historical-rpg-demo.css`

**资源文件夹**：
- `static/images/ruju-demo/` 整个文件夹
- `static/audio/ruju-demo/` 整个文件夹

**数据文件**（需要先 grep 确认）：
- `data/intrigue_scenes_*.json` 中非公开课的部分
- 旧 8 个入局场景数据（沙丘诏书、白登之后、推恩令、赤壁、玄武门、澶渊之盟、土木之变后、虎门禁烟前夜）

**HTML/JS 引用清理**：
- `static/index.html` 中所有 `historical_rpg_demo` 相关引用
- `static/js/app.js` 中所有 `historical_rpg_demo` 接入逻辑
- `api.py` 中的相关 payload 处理

### 阶段二保留的文件（虽然旧但不删）

- `career_engine.py`
- `data/career_offices.json`
- `data/career_identity_pools.json`
- `data/career_openings.json`
- `data/career_cases_tang_county_wei.json`
- `data/career_rules.json`
- `data/career_prototypes.json`

**理由**：用户未明确删除，且这些可作为后续"官制资料"参考。

### 阶段三将新建的文件夹

```
docs/
docs/chapters/
data/stories/
data/stories/sanguo/
data/stories/sanguo/chapters/
data/stories/sanguo/prompts/
static/images/sanguo/
static/audio/sanguo/
static/js/intrigue/             ← 新建入局模块前端
static/css/intrigue/            ← 新建入局模块样式
```

---

## 附录 D：UI 重做的具体技术建议

按 `docs/04_UI_SPEC.md`（即 PROJECT_BIBLE 第七节）实现时，建议技术路径：

1. **全屏画面 + 字幕式文字**：纯 CSS 即可，不需要 Canvas
2. **"印章式"选择按钮**：CSS `border-radius: 50%` + 自定义 hover 动画
3. **"言"按钮的仿古纸输入框**：CSS overlay + 自定义字体（毛笔字效果可用 `font-family: '楷体', 'KaiTi'` 或导入 web 字体）
4. **章节切换的地图过场**：SVG + CSS 动画（路径动画）
5. **笔记本翻页**：CSS 3D transform 或者用 `turn.js` 等库

不需要引入大框架（React/Vue），保持现有 vanilla JS 风格即可。

---

## 最后

读完本文档，你已经知道：

- ✅ 项目是什么
- ✅ 用户是谁、他的偏好
- ✅ 该做什么、不该做什么
- ✅ 五个阶段的实施顺序
- ✅ 第一步该问什么

**请开始执行第六节"接手时第一步"。**

完成读文档+扫描项目+汇报后，等用户回应再继续。
