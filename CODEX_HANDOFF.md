# 时空印证系统：新对话交接说明

更新时间：2026-05-20  
项目路径：`E:\gitclone\history-app2\history-app`

这个文件是给下一次新开的 Codex 对话看的。新对话开始后，可以先让 Codex 读取这个文件，再继续改项目。

## 一句话定位

这是一个中国历史事件学习网站，名字叫“时空印证系统”。核心不是单纯讲故事，也不是单纯 AI 聊天，而是帮助用户先搭建中国历史的“骨架”：按朝代理解关键转折事件，再通过事件故事、人物对话和总结页逐步填充细节。

## 用户偏好

- 用户没有编程经验，希望解释尽量用普通中文，不要堆技术术语。
- 用户通常希望 Codex 直接修改代码，而不是只给方案。
- 不要随便推送 GitHub。只有用户明确说“推送”时再提交和推送。
- 用户的后端部署在 Render，Render 会跟 GitHub `main` 分支自动部署。
- 用户很在意界面观感，喜欢历史感、纸张感、深红黑金的国风，但不喜欢太亮太白、太像模板。
- 用户希望内容基于正史，不能为了戏剧化胡编，也要避免明显时间线错误。

## 当前主要文件

- `api.py`：FastAPI 后端，事件接口、聊天接口、猜人物游戏、统计后台、反馈接口、数据库持久化逻辑都在这里。
- `events.yaml`：历史事件内容库，包含标题、时间、地点、故事、角色、下一案、史料说明、AI 提示等。
- `static/index.html`：主页面结构。
- `static/css/styles.css`：网站主要样式，包括首页、事件页、秦朝骨架页、移动端适配。
- `static/js/app.js`：前端逻辑，包括导航、事件加载、聊天、流式输出、猜人物游戏、反馈弹窗、首页入口。
- `static/images/`：本地图片资源。目前有 `qin-skeleton-bg.png.png` 和 `home-hero-bg.png`。

## 当前 Git 状态

最近已推送的提交大致包括：

- `a5f29e3 Redesign Qin skeleton archive page`
- `6650b85 Polish Qin skeleton archive layout`
- `bdd3c12 Widen Qin skeleton archive layout`

截至写这个文件时，本地还有未提交改动：

- `api.py`
- `events.yaml`
- `static/css/styles.css`
- `static/index.html`
- `static/js/app.js`
- `static/images/home-hero-bg.png` 是新文件

这些改动主要是：首页、本地首页背景图、首页按钮适配、秦朝骨架文字微调、导航排序、以及“子婴降秦”更正为“子婴降刘”。用户要求首页先本地测试，不要立刻推送。

## 已完成的重要功能

### 事件故事页

- 左侧导航按朝代分组。
- 每个历史事件有标题、时间、地点、人物、图片、正文故事。
- 事件末尾可以配置“下一案”，用户点击后跳到相关事件。
- 右侧是实时对话区，可以围绕当前事件向人物提问。

### 对话功能

- 支持流式输出。
- 支持点击人物名进行单聊。
- 群聊模式会让多个相关人物依次回答。
- 增加了提示文字，告诉用户“点人物名可单聊，群聊模式会让多位相关人物依次回应”。
- 回答方式有两种：
  - 快速：大约 10 秒，使用 `qwen-turbo`
  - 专家：大约 45 秒，使用 `qwen3.6-plus`
- 曾经存在“每次回答都反问用户”的问题，已经通过提示词和逻辑调整为更自然，不强制每轮都提问。

### 猜历史人物游戏

入口在左侧导航“互动游戏 / 猜历史人物”。

玩法大意：

- 用户先设定一个自己心里的人物。
- AI 自动设定一个中国历史人物。
- 双方轮流通过问题缩小范围。
- 后来从“只能回答是/不是”调整为“简短裁判式回答”，因为纯是/否缺少教育意义。
- 支持用户忘记人物时一直显示“你的角色”。
- 支持“揭晓答案”按钮。

注意：

- AI 不能在回答用户问题时泄露自己设定的人物。
- 对人物时代归属要谨慎，比如成吉思汗与元朝的关系容易引发用户疑问。

### 后台统计与反馈

- 有一个 admin 后台，统计访问、独立访客、事件浏览、AI 对话、用户提问、猜人物互动、反馈等。
- 后台只统计匿名访问和互动次数，不展示完整聊天内容时曾经更克制；后来用户希望记录用户给 AI 提了什么问题，因此已开始记录提问内容或相关动作。
- 增加了反馈入口，用户可以留言。
- 反馈可在 admin 后台看到。
- 左侧导航底部增加了 QQ 群：`1073677465`。
- 移动端曾出现反馈按钮被遮挡，已经做过调整。

### 数据库持久化

- 之前 admin 数据每次 Render 重启会清空，后来改成持久化数据库。
- Render 上创建了 Postgres，数据库名大概是 `admin comments`。
- Web Service 环境变量里设置了 `DATABASE_URL`。
- 不要把数据库密码或 API Key 写入代码或交接文件。
- Render 免费 Postgres 会过期，用户后来成功创建了 Free Postgres，但需要注意 Render 页面提示的到期时间。

### 秦朝骨架页

用户提出核心产品思路：按朝代像楼层一样搭历史架子。秦朝是一楼，先读若干关键事件，再进入“秦朝骨架”总结页，帮助用户整体理解秦朝如何建立、如何崩塌。

秦朝当前事件大致包括：

- 一统六国
- 始皇称帝
- 郡县天下
- 书同文车同轨
- 北击匈奴
- 南征百越
- 焚书坑儒
- 沙丘之变
- 大泽乡起义
- 巨鹿之战
- 子婴降刘
- 秦朝骨架

秦朝骨架页已经做成视觉化总结页，包括：

- 顶部历史感 hero 图
- “帝国主线：大秦兴亡十一章”
- 11 个主线节点卡片
- “秦朝为何能迅速兴起？”
- “帝国裂痕从哪里开始？”
- “史官结语”

用户反馈过的问题及已调整方向：

- 页面不要太窄，中间区域要更宽。
- 卡片不要太白太亮，要更暗、更有历史感。
- 主线卡片小字不要太粗，已经改成更轻的宋体正文感。
- 顶部图要保留比较重的历史质感，不要洗得太白。
- “秦朝骨架”应放在秦朝导航最后，不要放第二个。

### 首页

用户希望新增网站首页，但先本地测试，不要急着推 GitHub。

首页目标：

- 背景图使用 `static/images/home-hero-bg.png`。
- 背景图本身已经带有大标题“穿越千年，重返历史现场”和说明文字。
- 页面只保留“开始探索”按钮，不再叠加额外标题文字。
- 点击“开始探索”后进入事件页，默认从“秦朝·一统六国”开始。
- 首页按钮需要适配不同屏幕尺寸和手机屏幕，避免压住背景图里的文字。
- 为避免浏览器缓存旧 CSS，`static/index.html` 里 CSS/JS 链接加过版本参数。

## 产品思路与重要讨论

用户最初考虑过历史推演，但后来觉得推演自由度太高，普通用户没有足够知识储备很难玩到点子上，而且“历史已经发生过”，推演意义有限。

后来产品核心逐渐明确为：

- 用户不是为“读单个历史事件”付费。
- 用户也不是为“和 AI 聊天”付费。
- 用户真正可能付费的是：建立一套中国历史世界观和结构。
- 网站应该帮用户把历史“架子”搭起来，之后用户再去用豆包、DeepSeek 等 AI 深挖细节时，才知道该问什么。

用户常用比喻：

- 朝代像楼层：秦朝是一楼，汉朝是二楼。
- 每一层要先搭好结构，再慢慢填墙和装修。
- 很多人知道安史之乱、赤壁之战，但不知道陈桥兵变、八王之乱、大泽乡起义这些“四字事件”背后代表的历史转折。
- 如果用户连事件名都不知道，就不会主动去问 AI。因此网站的价值在于帮用户发现这些节点。

## 内容原则

- 要基于正史，尽量参考《史记》《汉书》等传统史料线索。
- 可以做现代结构化分析，但要区分“史书记载”和“现代分析判断”。
- 不要把现代总结说成史书原文。
- 不要把复杂灭亡原因压缩成单一原因。
- 不要为了爽感乱写。
- 对时间线要非常谨慎。

一个已发生的命名修正：

- 原来有“子婴降秦”，用户指出不对。
- 不建议改成“子婴降汉”，因为当时刘邦还没有正式建立汉朝。
- 当前改为“子婴降刘”，更接近“子婴向刘邦投降”的意思。

## 技术与本地开发注意

### 本地运行

常用本地启动：

```powershell
python -m uvicorn api:app --host 127.0.0.1 --port 8001
```

本地访问：

```text
http://127.0.0.1:8001/
```

如果 URL 后面带 `#秦朝·一统六国` 之类 hash，会直接进入事件页，不会停留在首页。

### 检查方式

可用：

```powershell
python -B -c "import ast, pathlib; ast.parse(pathlib.Path('api.py').read_text(encoding='utf-8')); print('api syntax ok')"
```

也可用 FastAPI TestClient 检查 `/events_list` 是否正常。

注意：

- 在这台电脑上执行 `node --check static/js/app.js` 时曾经报 `Access is denied`，即使请求权限也失败。可以改用 Node REPL 或浏览器检查 JS。
- 本地有时 8001 服务会停，浏览器打不开时先确认 uvicorn 是否还在运行。

### 编码与 PowerShell

PowerShell 输出中文有时会乱码，但文件本身是 UTF-8。读文件时尽量加 `-Encoding UTF8`。

Git 提示 `LF will be replaced by CRLF` 是 Windows 换行提示，通常不影响提交。

## 协作规则

新对话里的 Codex 应该这样工作：

1. 先读这个 `CODEX_HANDOFF.md`。
2. 再看 `git status --short`，确认当前本地有哪些未提交改动。
3. 如果用户让改 UI，优先改现有 `static/index.html`、`static/css/styles.css`、`static/js/app.js`，不要重新造一套页面。
4. 如果用户让改历史内容，优先改 `events.yaml`，并注意 `next_event`、导航标题、骨架节点引用要同步。
5. 如果用户让推送 GitHub，再执行提交和推送；否则只本地修改。
6. 给用户解释时尽量说“我改了什么、你怎么本地看”，不要展开太多技术细节。

## 当前最可能继续做的事

- 调整首页按钮在不同屏幕上的位置。
- 本地测试首页后，用户可能会要求提交并推送 GitHub。
- 继续完善秦朝骨架页视觉。
- 给其他朝代也做类似“骨架总结页”。
- 继续补充各朝代关键事件。
- 继续优化移动端体验。

## 2026-05-20 本轮继续修改记录

- 首页入口已固定为“秦朝·一统六国”，不再依赖事件列表中的第一个非总结事件。
- 首页窄屏竖屏样式已调整：背景图优先完整显示标题和说明，按钮下移到图下方，避免压住图中文字。
- `static/index.html` 的页面标题已统一为“时空印证系统”，CSS/JS 版本参数更新为 `home-polish-20260520`。
- 秦朝骨架导航仍保持在秦朝档案最后；`/events_list` 已检查通过。
- 若未来某个朝代骨架总结页没有配置 `characters`，前端会禁用提问并提示用户先进入具体事件，避免空人物对话报错。
- 本地 SQLite 统计库出现过 `disk I/O error`。现在 `/analytics/visit` 和 `/analytics/event` 即使统计写入失败也会返回成功，避免影响用户浏览；AI 对话中的统计和提问记录也改为失败不打断主流程。
- 后台统计页说明已更新：会展示用户提问，但不展示 AI 完整回答和整段聊天记录。
