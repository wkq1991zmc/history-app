# 时空印证系统：Codex 交接说明

更新时间：2026-05-27
项目路径：`E:\gitclone\history-app2\history-app`
线上仓库：`https://github.com/wkq1991zmc/history-app`

这个文件给下一次新开的 Codex 对话使用。新对话开始后，先读这个文件，再看 `git status --short`，就能接上当前项目状态。

## 2026-05-27 最新状态（新对话优先读）

这一段是最近几天连续工作的总交接。旧章节里有些内容是历史背景，若与本段冲突，以本段为准。

### 当前产品判断

- 项目已经从“单纯历史故事 + AI 聊天”推进到三个核心体验：
  - 主线历史长卷：免费阅读，负责建立朝代与关键事件骨架。
  - 事件对话：用户围绕具体事件问历史人物，登录后继续深聊。
  - 入局玩法：用户进入历史决策现场，作为决策者追问臣子并发布最终决定，由 AI 推演分支。
- 用户认可的收费设想：
  - 主线故事免费。
  - 对话功能允许游客先体验 1 次，再继续对话需要注册 / 登录。
  - 入局模式登录用户可玩基础剧本，付费后解锁更多剧本。
  - 暂未接支付；付费只是产品方向，不要贸然实现。
- 用户担心普通 AI 也能回答历史问题，所以项目的价值不应只押在“问答”上，而应押在：
  - 结构化历史事件库。
  - 正史骨架和故事厚度。
  - 入局这种带角色、局势、知识边界和分支推演的玩法。
  - 网站整体视觉和叙事氛围。

### 当前 Git 与本地改动

- 当前分支：`main`。
- 最近已推送到 `main` 的提交包括：
  - `41875b6 Use Aliyun DirectMail API for login emails`
  - `c03002b Require login after one guest chat`
  - `2a12ee0 Add registration stats to admin analytics`
  - `37c49c4 Expand Three Kingdoms event archive`
  - `1de56d8 Fix dynasty skeleton archive titles`
- 当前本地还有未提交改动，主要是“电影感黑金 UI”这一轮：
  - `static/index.html`
  - `static/css/styles.css`
  - `static/js/app.js`
- 这些 UI 改动还没有推送。除非用户明确说“推送 GitHub”，不要提交或推送。
- 本轮检查已通过：
  - `python -m py_compile api.py app.py load_data.py bot.py`
  - `git diff --check` 只有 Windows 换行提示，没有空白错误。

### 登录与邮件验证码

- 用户购买了域名：`qianqiulu.com`。
- 阿里云邮件推送已配置发信域名：`mail.qianqiulu.com`，DKIM / SPF / DMARC / MX 最终验证通过。
- Render 免费版不能稳定使用 SMTP 出站端口，QQ 邮箱 SMTP 和阿里云 SMTP 都不应作为线上主方案。
- 现在已改为阿里云 DirectMail API，验证码邮件已在 Render 线上成功收到。
- 关键点：
  - 必须使用 DirectMail API，不要回退 SMTP。
  - Render 环境变量需要 DirectMail 相关配置，包括 endpoint。
  - 曾经因为漏填 `ALIYUN_DM_ENDPOINT=dm.aliyuncs.com` 卡了很久，后面补上才成功。
  - `ALIYUN_DM_FROM_ALIAS` 可以填中文显示名，例如“千秋录”；这是邮件显示名，不是登录账号。
  - 不要把用户真实 AccessKey、密码、验证码、邮箱等敏感内容写进代码、提交、文档或回复。
- 当前登录策略：
  - 游客可以不登录对话 1 次。
  - 第 2 次继续对话时弹出邮箱验证码登录。
  - 登录 / 注册是同一个流程：邮箱收 6 位验证码，验证后创建或登录账号。
  - 后台已加“每日注册用户数”统计。

### 入局玩法状态

- 玩法已经从“玩家扮演臣子说服君主”改为更稳定的“玩家扮演最终决策者”。
- 原因：让玩家扮演臣子时，AI 很难稳定判断玩家的发言、君主的思考、多方讨论和历史分支，容易答非所问。
- 当前入局模式：
  - AI 先让臣子 / 将领 / 相关人物进行数轮讨论。
  - 玩家作为决策者可以追问、质疑、要求继续争论。
  - 右侧或下方有“决定”按钮，弹窗中输入正式决定。
  - 用户发布决定后，本局结束，由旁白进行历史分支推演。
- 已做过的重要修正：
  - 不靠关键词判断“用户是否已经决定”，改成显式“决定”按钮。
  - 不再固定 3 轮自动结束。
  - 玩家输入“1”或无关内容时，AI 应该追问其意图，而不是自作主张推进。
  - 角色必须遵守知识边界，例如沙丘诏书局中，局中人不能提前知道秦始皇已死。
  - 开局臣子意见和旁白推演不应全部预写死，应该依靠事件卡 + 通用规则 + AI 生成。
- 模型体验：
  - `qwen3.6plus` 的入局质量更好，但速度慢。
  - 后来尝试 `flash`，速度更适合普通体验。
  - 不要只靠模型硬顶，关键是事件卡结构、知识边界和分支推演规则。
- 入局事件卡在 `data/intrigue_scenes*.json`，说明见 `data/README.md`。
- `api.py` 中已有入局事件卡校验、知识边界、玩家意图裁判、朝议推进和最终推演相关逻辑。
- 已补过一批高质量入局事件，并把前情提要写厚；用户曾反馈前情提要没变化，说明改数据后要确认前端实际加载的是新字段。

### 历史内容状态

- 秦朝、汉朝主线已经比较完整。
- 汉朝 20 个主线事件已经扩写过，部分支线保留为支线。
- 三国已按“秦朝 / 汉朝骨架”的方式补充事件，并写厚现有 4 个事件。
- 三国内容原则：
  - 以《三国志》《资治通鉴》等正史线索为主。
  - 可以吸收通俗叙事，但不要把《三国演义》情节当正史。
  - 如果是后世概括或现代判断，要和史书记载区分。
- 最近修过一个骨架页错误：
  - 汉朝骨架和三国骨架不应显示“秦朝为何能迅速兴起？”
  - 三国右侧不应写“帝国裂痕”，因为三国不能简单算帝国。
  - 已提交 `1de56d8 Fix dynasty skeleton archive titles`。

### 当前 UI 方向：电影感黑金版

用户给了两张理想 UI 图，方向是：

- 黑底、金字、暗红高亮。
- 像“历史档案库 / 史案驾驶舱 / 文明卷宗系统”。
- 左侧是档案目录，中间是事件故事，右侧是 AI 对话。
- 顶部有导航和搜索，整体更适合录视频。

当前本地未提交 UI 改动已经实现：

- 顶部导航改为：
  - 首页
  - 历史长卷
  - 互动游戏
  - 入局
- 顶部导航当前行为：
  - 首页：回到“穿越千年，重返历史现场”的首页。
  - 历史长卷：进入主线故事默认事件。
  - 互动游戏：进入猜历史人物试玩页，并显示“玩法开发中 / 试玩版”提示。
  - 入局：只弹出“正在开发中，暂未开放试玩”提示，不进入真实入局界面。
- 左侧导航：
  - 移除了“互动游戏”和“历史人物”入口。
  - 档案前的圆圈改成小金色印记。
  - 左侧只保留朝代档案结构。
- 中间故事区：
  - 框体变宽。
  - 正文字号放大到约 18px。
  - 图片下方“发生时间 / 案发地点 / 卷宗涉案人”已对齐在同一水平线。
  - 故事正文里的关键词加粗和变色已压成统一正文样式。
- 右侧聊天区：
  - 从青蓝科幻改为黑金暗红，和中间故事、左侧导航统一。
  - 保留少量未来科技感：线框、微光、环形纹理。
  - 用户和 AI 回复去掉突兀白底，更融入整体。
  - 输入框文字颜色修正，之前用户输入不容易看清。
- 当前访问：
  - `http://127.0.0.1:8001/?cinematic=1#秦朝·书同文车同轨`

### 最新滚动问题与修复

用户发现右侧群聊内容变多后页面像被锁死。实际根因：

- 右侧聊天记录区在 flex 布局里缺少 `min-height: 0`。
- 群聊内容一多，聊天记录区被内容撑高，不再内部滚动。
- 外层页面又是固定高度，所以看起来整个页面被锁死。

已修复：

- `#chat-section` 设置 `overflow: hidden`。
- `#chat-history-container` 设置固定 flex 内部滚动：
  - `flex: 1 1 0`
  - `min-height: 0`
  - `overflow-y: auto`
- AI 流式回复时，如果用户正在向上翻聊天，不再强行把聊天区拉回底部。

曾经短暂加过“右侧滚到顶部后把滚轮交给中间故事区”的逻辑，但用户反馈不合理：

- 鼠标在右侧对话框里，就不应该滚动中间事件。
- 这段交接逻辑已经撤掉。
- 现在的规则应是：
  - 鼠标在右侧，只滚右侧。
  - 鼠标在中间，只滚中间。

最新缓存版本：

- `cinematic-scrolllocal-20260527`

### 视觉与体验的当前待办

如果明天继续 UI，优先让用户看当前本地效果，再按截图反馈微调。可能还要处理：

- 右侧对话区字体、间距、按钮是否够清晰。
- 中间故事标题、图片比例、元信息栏在不同分辨率下是否稳。
- 顶部导航的“历史长卷 / 互动游戏 / 入局”是否需要落到真实页面，而不是只是切换旧功能。
- 移动端是否要单独做电影感版本。目前主要改的是桌面录视频效果。

### Codex 配置与对话管理背景

- 用户之前遇到 Codex 配置提示：`[features].codex_hooks is deprecated`，应改用 `[features].hooks`。
- “归档对话”只是把旧对话收起来，适合长线程结束后整理，不会自动删除项目文件。
- 用户右下角“上下文是否需要自动压缩”的图标一度消失，这属于 Codex 客户端体验，不是项目代码问题。

## 一句话定位

这是一个中国历史学习网站，名字叫“时空印证系统”。核心不是单纯讲故事，也不是只做 AI 聊天，而是帮助用户按朝代搭起中国历史的“骨架”：先知道关键事件，再通过事件故事、人物对话、朝代骨架页和互动玩法逐步补细节。

用户现在最看重的产品方向是：让普通用户先看到历史主线节点，知道“有哪些事情值得问”，再用 AI 对话深入理解。

## 用户偏好

- 用户没有编程背景，解释尽量用普通中文，不要堆技术术语。
- 用户通常希望 Codex 直接改项目，而不是只给方案。
- 不要随便推送 GitHub。只有用户明确说“推送 GitHub”时再提交和推送。
- Render 会跟 GitHub `main` 自动部署，所以推送前要确认改动真的适合上线。
- 用户很在意视觉气质：历史感、纸张感、深红黑金、国风，但不喜欢太亮、太白、太模板。
- 内容必须尽量基于正史，尤其是《史记》《汉书》《后汉书》《三国志》《资治通鉴》等线索。可以现代分析，但要区分“史书记载”和“现代判断”。
- 用户对访问转化非常敏感。强制注册会明显降低用户对话意愿，目前产品策略是先体验、后登录。

## 主要文件

- `api.py`：FastAPI 后端。事件接口、聊天接口、穿越 demo、猜人物、统计后台、反馈、登录相关接口都在这里。
- `events.yaml`：历史事件内容库。标题、时间、地点、故事、人物、下一案、史料说明、AI 提示等都在这里。
- `static/index.html`：主页面结构。
- `static/css/styles.css`：主要样式，包括首页、事件页、朝代骨架页、移动端适配、穿越 demo 样式。
- `static/js/app.js`：前端逻辑，包括导航、事件加载、聊天、流式输出、猜人物、反馈弹窗、首页入口、游客对话次数等。
- `static/images/`：本地图片资源：首页桌面和移动端背景图等。

## 当前 Git 状态

截至 2026-05-27，最近已经推送到 GitHub `main` 的提交包括：

- `1de56d8 Fix dynasty skeleton archive titles`
- `37c49c4 Expand Three Kingdoms event archive`
- `2a12ee0 Add registration stats to admin analytics`
- `c03002b Require login after one guest chat`
- `41875b6 Use Aliyun DirectMail API for login emails`

当前本地还有未提交的电影感 UI 与本文件更新。除非用户要求推送，不要提交或推送。

## 当前运行方式

本地启动：

```powershell
python -m uvicorn api:app --host 127.0.0.1 --port 8001
```

本地访问：

```text
http://127.0.0.1:8001/
```

如果浏览器打不开本地页面，先确认 8001 的 uvicorn 是否还在运行。

常用检查：

```powershell
python -m py_compile api.py
```

也可以请求：

```text
http://127.0.0.1:8001/events_list
```

注意：PowerShell 有时显示中文乱码，但文件本身通常是 UTF-8。不要因为终端乱码就判断数据坏了。

## 首页与导航

首页已经完成：

- 桌面背景图使用 `static/images/home-hero-bg2...` 系列资源。
- 移动端背景图使用 `static/images/home-hero-bg-mobile...` 系列资源。
- 标题“穿越千年，重返历史现场”和副标题现在由代码渲染，不再烙在图里。
- “开始探索”按钮是圆角样式，位置已经上调。
- 移动端曾讨论过竖版背景图，用户已经放入移动端资源。
- 首页右上角的“登录 / 注册”按钮已删除。
- 现在点击“开始探索”会直接进入事件内容，不再强制登录。

直接输入事件 hash 链接时：

- 允许用户直接看事件。
- 不再因为未登录而强行回首页。
- 之前的登录拦截已经撤下，因为国内用户访问 Firebase 可能被网络环境卡死。

## 登录系统状态

曾经做过几轮登录方案：

1. 自研邮箱验证码登录：
   - 后端接口包括 `/auth/email/request_code`、`/auth/email/login`、`/auth/me`、`/auth/logout`。
   - QQ 邮箱 SMTP 在 Render 上不稳定，SMTP 端口可能被限制。
   - Brevo 账号显示 `Suspended`，不可用。

2. Firebase Email/Password：
   - 用户创建过 Firebase 项目 `history-app-3e6cb`。
   - 本地可注册登录。
   - 但国内用户不翻墙时 Firebase / Google 相关请求容易失败，严重影响首页和事件页体验。

当前策略：

- 用户可以直接看事件，不要在首页或事件阅读前强制登录。
- AI 对话允许游客先体验 1 次。
- 游客继续对话时再弹出邮箱验证码登录。
- 线上邮件验证码已从 SMTP 改为阿里云 DirectMail API，Render 已验证可收到邮件。
- 不要再依赖 Firebase 或 raw SMTP 作为核心登录链路。
- 阿里云邮件推送费用不是早期主要压力，先确保可用性和转化路径。

## 游客体验策略

曾经因为首页强制注册，统计后台显示访问很多但几乎没人对话。判断原因是：用户被注册登录卡住，尤其国内访问 Firebase 不稳定。

因此改成：

- 事件内容允许游客查看。
- AI 对话可以先让游客体验 1 次。
- 第 2 次继续对话时提示登录。
- 不要在用户打开事件或首页时自动弹注册框。
- 登录弹窗应该只在用户主动触发，比如超过免费次数或点击需要账号的功能时出现。

当前实际状态已改成“游客 1 次后登录”，见 `static/js/app.js` 的游客对话计数逻辑和 `api.py` 登录接口。

## AI 对话模型

当前后端聊天模型路由在 `api.py`：

- 专家模式：默认 `WEB_MODEL`，目前默认 `qwen3.6-plus`。
- 快速模式：默认 `WEB_FAST_MODEL`，目前已经改成 `qwen3.6-flash`。
- 快速模式默认关闭 thinking：
  - `WEB_FAST_ENABLE_THINKING` 为空或 false 时，`qwen3.x` 快速模式会传 `extra_body={"enable_thinking": False}`。
  - 这是关键，否则 `qwen3.6-flash` 会慢到十几秒。

本地测试结论：

- `qwen-turbo` 非常快，但回答偏浅，历史解释和穿越推进都比较模板化。
- `qwen3.6-flash` 默认 thinking 时质量好但很慢，首字可能 10 秒以上。
- `qwen3.6-flash + enable_thinking=false` 速度很好，首字约 0.3-1 秒，总耗时约 2-4 秒，质量明显好于 turbo。
- 已推送提交 `366748d Use Qwen flash for fast chat mode`。

专家模式 token：

- 曾经为了加速把专家模式 `max_tokens` 降过。
- 用户后来要求改回 `800`，已经完成并推送。
- 当前 `api.py` 里快速模式约 `480`，专家模式 `800`。

## 专家模式速度讨论

曾讨论过专家模式慢的原因：

- system prompt 太长。
- 事件深度上下文太重。
- 同时输出“半原文”和“白话文解”，生成量翻倍。
- 动态规则如果放在 system prompt 中，会影响模型缓存。

已经做过的方向：

- 部分动态规则移到最后用户消息附近，让 system prompt 更稳定。
- 专家模式仍保留较高质量和较长输出。

后续可继续优化：

- 进一步瘦身 `_format_event_deep_context`。
- 快速模式给更短但不浅的回答。
- 专家模式保留深度，避免变得太啰嗦。

## 历史事件内容

### 秦朝

秦朝主线已经相对成熟，当前主线大致包括：

- 一统六国
- 始皇称帝
- 郡县天下
- 书同文车同轨
- 北击匈奴
- 南征百越
- 焚书坑儒
- 沙丘之变
- 大泽乡起义
- 巨鹿之战
- 子婴降刘
- 秦朝骨架

用户曾指出“子婴降秦”不对，已改为“子婴降刘”，因为当时刘邦还没有正式建立汉朝，不宜叫“降汉”。

秦朝事件已经做过“写厚”处理，故事比最初更完整。用户特别关注措辞：

- “治理机器”一类偏硬的表达用户觉得疑惑，曾要求改掉。
- 巨鹿之战、子婴降刘中类似偏硬的表达已调整为更易懂的说法。

### 秦朝骨架页

秦朝骨架页已经做成视觉化总结页，类似一层楼的“历史结构图”。

特点：

- 历史感 hero 图。
- “帝国主线”节点卡。
- 解释秦朝为何迅速兴起、裂痕从哪里开始、史官结语。
- 用户曾要求页面不要太窄，中间区域要更宽。
- 卡片不要太白太亮，要更暗、更有历史感。
- 主线小字不要太粗。
- “秦朝骨架”放在秦朝导航最后。

### 汉朝

用户和 Codex 讨论后确定汉朝主线 20 个事件：

1. 楚汉之争
2. 刘邦称帝
3. 白登之围
4. 吕后临朝
5. 文景之治
6. 七国之乱
7. 汉武帝登基
8. 推恩令
9. 独尊儒术
10. 北击匈奴
11. 张骞通西域
12. 盐铁官营与轮台罪己诏
13. 霍光辅政
14. 昭宣中兴
15. 王莽篡汉
16. 光武中兴
17. 党锢之祸
18. 黄巾起义
19. 董卓入京
20. 汉献帝禅让

另外：

- `汉朝·长乐宫血案`
- `汉朝·巫蛊之祸`

这两个保留为汉朝支线档案，不放进 20 条主线。导航里应标为：

- 支线：长乐宫血案
- 支线：巫蛊之祸

用户曾指出导航顺序不对，比如推恩令后应该接独尊儒术，已经检查并修正过。

汉朝事件已在 2026-05-22/23 写厚一轮，提交 `0d03fb4 Expand Han dynasty event stories` 已推送。写法要求：

- 像秦朝一样厚一点。
- 基于正史脉络。
- 不要写成架空小说。
- 后汉部分要讲清外戚、宦官、士人、地方武装、强臣代政的因果链。

## 关键词加粗

用户希望故事正文里像“沙丘之变”一样有关键词加粗，方便阅读。

曾尝试过前端自动加粗，但效果不好：

- 自动规则把很多人名重复加粗。
- 还曾出现红色字或红色背景，用户不喜欢。

用户明确要求：

- 不要红色字。
- 不要红色方块背景。
- 只需要字体加粗、变黑。
- 人名不一定要加粗。
- 更应该加粗“法家治国理想”“个人宗族私欲”这种概念性关键词。

当前方向：

- 不建议再用简单人名匹配。
- 更适合在内容层手工加 `<b>`，或维护每个事件的概念关键词列表。
- 如果继续做自动加粗，需要以“概念短语”为条件，而不是人名。

## 猜历史人物小游戏

入口在左侧导航“互动游戏 / 猜历史人物”。

玩法：

- 用户心里想一个历史人物。
- AI 自动设定一个人物。
- 双方轮流问问题缩小范围。
- 后来从“只能回答是/不是”改为“简短裁判式回答”，因为纯是/否教育意义不强。
- 支持“揭晓答案”。

注意：

- AI 不能在回答用户问题时泄露自己设定的人物。
- 人物时代归属要谨慎，例如成吉思汗与元朝关系容易引发用户疑问。

后续优化方向：

- 提高猜人物的可玩性。
- 做更清晰的规则和回合反馈。
- 让答案和提示更有历史教育价值。

## 穿越玩法

用户提出了一个“文字版 GTA”的长期构想：

- 用户点击穿越按钮。
- AI 随机分配朝代、年份、年龄、性别、身高、体重、身份。
- 用户目标是活下去，也可以建功立业或苟且偷生。
- AI 根据当时正史背景随机生成事件和可选行动。
- 用户每次选择后，继续生成新处境和新行动，直到角色死亡或走向结局。
- 用户也可以和遇到的人物交谈，人物由 AI 扮演。

核心产品价值：

- 通过“自己穿越”的方式近距离接触正史背景。
- 补充 165 个主线事件没有覆盖到的历史细节。
- 不是纯爽文，不是乱改历史，而是在历史边界内体验个人命运。

当前状态：

- 已经做了一个隐藏的穿越 demo。
- 提交 `9d13a15 Add hidden time travel demo` 已推送。
- 代码可以保留并上传，但网站导航栏里不要显示“随机穿越”入口。
- 因为玩法还太早期，不要让普通用户明显看到入口。

用户对穿越 demo 的反馈：

- 不要用兜底剧情。
- 点击穿越按钮时应显示：
  - 正在加载朝代……
  - 正在加载人物……
  - 正在加载环境……
  - 最好有进度条。
- 之前 qwen3.6-plus 生成很慢，超时调长后能加载。
- 每次选择后不应该都等很久。
- 用户测试时遇到“不管选哪个都是推进失败”，无痕模式跑通，可能和浏览器缓存或旧状态有关。
- 后续选择推进有时牛头不对马嘴，说明上下文承接和状态更新还不稳。
- 用户后来觉得这个玩法太复杂，暂时放一放，先做历史内容。

关于模型：

- 穿越开局可用 `qwen3.6-plus` 或更强模型。
- 普通选择推进、NPC 短对话可以考虑 `qwen3.6-flash` 关闭 thinking。
- 最重要不是无脑换模型，而是拆任务：
  - 开局生成世界。
  - 选择推进只生成本次后果和下一组选项。
  - NPC 对话只围绕当前人物和场景。
  - 历史校验用固定背景资料限制边界。

### 2026-05-23 新穿越方向：从开放世界改为“入局”

用户提出了新的想法：暂时不要让 AI 随机生成完整人物和开放剧情。新玩法应收束为“进入一个历史现场，成为局中某个角色，参与朝堂/军议/密谈，然后由其他历史人物回应，最后由旁白做推演”。

这个方向比旧的“文字版 GTA”更适合当前项目，因为：

- 不需要 AI 从零生成大世界，稳定性更高。
- 场景基于主线事件和正史背景，历史边界更清楚。
- 每次只生成一个角色反馈或一段旁白，速度更快。
- 和现有事件库强绑定，可以把 165 个事件逐步转成可互动场景。
- 更适合视频演示：用户一眼能看懂“我穿越成某个历史人物，正在参与真实历史决策”。

建议把玩法命名为：

- “入局”
- 或“历史决策局”
- 不建议第一版继续强调“开放穿越”，否则用户会期待自由冒险和无限世界。

核心体验：

1. 用户点击“随机入局”。
2. 系统从预设场景库抽取一个历史现场。
3. 系统随机分配一个可扮演角色，不一定是皇帝，也可能是臣子、将领、谋士、使者。
4. 页面展示：
   - 时代
   - 地点
   - 当前局势
   - 你是谁
   - 你的身份、权力、立场和目标
5. NPC 先抛出一个议题或奏对。
6. 用户在对话框中自由输入自己的想法。
7. 当前 NPC 根据用户内容反馈。
8. 其他人物可以插话，例如“臣有事启奏”。
9. 一轮或数轮后，旁白站在上帝视角推演后果，并与正史做对照。

关键原则：

- 不是让用户随便改写历史，而是在真实历史压力下做角色扮演。
- 旁白不能直接说“历史已经被你改变”，应使用“如果按这个决策推演，可能会……”。
- 必须补充“正史中实际发生了什么”，保持教育价值。
- 用户输入离谱时，NPC 或旁白应指出不符合身份、时代或现实条件。

不要做成固定台词剧本。正确结构是：

```yaml
scene_id: han_baideng_court
title: 白登之后的朝议
historical_anchor:
  - 白登之围后，汉初对匈奴转向和亲与休养生息
  - 汉初国力未稳，诸侯王、功臣、边患同时存在
roles:
  - name: 刘邦
    identity: 皇帝
    goal: 稳住汉朝，不丢皇帝威望
  - name: 萧何
    identity: 丞相，掌后方与财政
    goal: 保护民力和财政，避免仓促再战
  - name: 樊哙
    identity: 武将
    goal: 维护军心，争取强硬姿态
  - name: 张良
    identity: 谋士
    goal: 避免战略冒进，寻找折中方案
possible_openings:
  - 萧何先启奏关中粮仓不足
  - 樊哙先请战，认为白登之辱不可不雪
  - 张良先提醒内有诸侯、外有匈奴，不宜双线冒险
  - 边使急报匈奴入边，朝堂争论战与和
hard_limits:
  - 不允许现代技术或现代知识介入
  - 不允许违背人物基本身份和职责
  - 必须说明正史中白登后汉初选择和亲与休养
```

这样同一个场景可以有多次不同体验：

- 第一次用户可能是刘邦，需要拍板。
- 第二次用户可能是萧何，需要说服刘邦暂缓大规模北伐。
- 第三次用户可能是樊哙，需要表达武将主战立场。
- 第四次用户可能是张良，需要权衡战、和、守、拖。

同一个“白登之后朝议”也能有不同开局：

- 萧何先谈财政。
- 樊哙先请战。
- 张良先谈战略。
- 边使急报先触发危机。

这解决了用户担心的“抽到同一个内容会减分”的问题：历史骨架固定，但现场切口、玩家角色、NPC 先后发言和旁白推演动态变化。

第一版建议只做 2-5 个高质量场景，而不是做很多浅场景。候选场景：

- `秦朝·沙丘之变`：用户可能扮演扶苏、蒙恬、使者等。议题是是否接受赐死诏书、如何判断诏书真假。
- `汉朝·白登之围`或白登之后朝议：用户可能扮演刘邦、萧何、樊哙、张良、陈平。议题是对匈奴战、和、守、拖。
- `汉朝·推恩令`：用户可能扮演汉武帝、主父偃、诸侯王代表、朝臣。议题是如何削弱诸侯而不激起叛乱。

可以加入评分，但第一版不必复杂：

- 史实贴合度
- 角色立场贴合度
- 说服力
- 风险控制
- 与正史差异

如果用户扮演萧何却喊“臣愿率兵北伐匈奴”，旁白应提示：这虽然能振奋军心，但不符合萧何作为后方财政中枢的职责，刘邦未必会认真采纳。

这套玩法本质不是“开放世界求生”，而是：

> 你进入一场真实历史局势，成为局中人，用自己的话参与一次历史决策。

## 视频与比赛

用户想录一个产品视频：

- 可以录屏，然后输入文案自动生成语音讲解。
- 曾讨论过剪映、CapCut、HeyGen、即梦/可灵/通义万相等方向。
- 用户希望视频里包含穿越 demo，哪怕只是一个小样本。

比赛讨论：

- 用户只能参加国内比赛。
- 曾看过江苏生成式人工智能创新大赛、AIGC For Future 等。
- 用户觉得很多比赛更偏机器人、卫星遥感等硬科技，项目不一定匹配。
- 如果继续找比赛，应优先找：
  - AIGC 应用创新
  - 教育科技
  - 数字文化
  - 文旅融合
  - 青年/个人开发者
  - 创新创业轻量产品

不要盲目鼓励用户“肯定适合”，要诚实判断匹配度。

## Gemini 技术债建议与当前判断

2026-05-23，用户贴过 Gemini 对项目的“核心技术债务修补与架构重构指南”，包括五个方向。下面是建议内容和当前取舍判断，方便新对话继续。

### 1. 全局状态易失性与进程内内存治理

Gemini 建议：

- `api.py` 中的 `rate_limit_store`、`guess_game_sessions`、`time_travel_sessions` 都依赖 Python 进程内 dict。
- Render 免费/弹性实例会休眠、重启或漂移，进程内状态会丢。
- 建议引入 Redis，或复用 PostgreSQL/SQLite，把 Session 状态持久化，提升无状态扩展能力。

当前判断：

- 这个方向是对的，但要分优先级。
- `rate_limit_store` 放内存问题不大，重启后重新计数，不会伤害核心体验。
- `guess_game_sessions` 和 `time_travel_sessions` 如果未来成为正式玩法，就不应该长期放内存；否则用户玩到一半，Render 重启后会丢局。
- 当前穿越 demo 仍隐藏，猜人物也偏轻量，不建议马上引入 Redis。
- 更适合先复用现有 PostgreSQL/SQLite，建一张轻量 session 表：

```text
game_sessions
- session_id
- type
- user_id / visitor_id
- state_json
- updated_at
- expires_at
```

优先级判断：

- 中等。
- 等“入局”或猜人物要认真做留存/继续游戏时再做。

### 2. 聊天历史从本地 JSON 改为数据库

Gemini 建议：

- 如果系统把 `chat_history` 写入 `chat_data/{user_id}/{event_name}.json`，Render 的临时文件系统会导致部署或容器重建后数据丢失。
- 建议废除本地 JSON 文件方案，改用数据库表，例如 `user_chat_histories`。

当前判断：

- 这是一个真实隐患。
- Render 本地文件系统不能当长期数据仓库。
- 如果未来要做“我的学习记录”“上次聊到哪里”“收藏问题”“登录用户历史”等功能，对话记录必须进数据库。
- 但如果当前网页端只是把最近几轮 history 放在前端请求里，后端不依赖长期 chat history，则短期不急。

更推荐的表结构不是存一大坨 JSON，而是按消息存：

```text
user_chat_messages
- id
- user_id / visitor_id
- event_name
- character
- role
- content
- answer_mode
- created_at
```

这样更利于统计、检索、用户历史页和后续分析。

优先级判断：

- 中高。
- 重新做登录系统、学习记录、用户画像之前必须做。

### 3. AI 回复改为结构化输出 + Pydantic 校验

Gemini 建议：

- 当前 `_extract_json_fields` 用大量正则和字符替换清洗模型返回的 JSON，脆弱。
- 建议启用 `response_format={"type": "json_object"}`。
- 用 Pydantic Schema 约束 `original_voice` 和 `modern_explain`。
- 删除手工正则解析。

当前判断：

- 这是最值得近期做的一项。
- 好处是减少模型格式漂移导致的解析失败、空回复和 500。
- 特别是快速模式已经换成 `qwen3.6-flash`，更应该把输出格式约束住。
- 但 Gemini 说“100% 合法 JSON”过于乐观，DashScope/OpenAI 兼容接口对不同 Qwen 模型的 `response_format` 支持需要实测。
- 不建议一上来删除旧解析逻辑。

推荐做法：

1. 定义 Pydantic 模型：

```python
class ChatReply(BaseModel):
    original_voice: str
    modern_explain: str
```

2. 调模型时尽量加：

```python
response_format={"type": "json_object"}
```

3. 返回后用 Pydantic 校验。
4. 校验失败时 fallback 到现有 `_extract_json_fields`。
5. 稳定一段时间后，再考虑移除旧正则清洗。

优先级判断：

- 高。
- 下一次有足够额度时，建议优先做。
- 先只改网页聊天接口，不动穿越和小游戏。
- 本地测试快速模式、专家模式各 2-3 条。

用户后来问过“这个修改的好处是什么”，解释是：

- 现在像让 AI 写一张表，然后后端拿剪刀从一堆文字里剪字段。
- 改完后像让 AI 直接填表单，后端检查表单有没有填完整。
- 用户体感是更少“服务端开了个小差”、更少空回复、更少格式怪异。

### 4. 群聊博弈引擎并发优化

Gemini 建议：

- 群聊多角色现在可能串行请求，4 个角色要等 4 次网络 I/O，延迟可到 10-20 秒。
- 建议用 `asyncio.gather` 并发。
- 每个角色给出“本轮反驳意愿值 0-100”。
- 后端筛选最高 1-2 个角色优先表态或交叉反驳。

当前判断：

- 方向有潜力，但 Gemini 写得偏复杂。
- 单纯并发生成 4 个角色的完整回答有问题：角色彼此不知道前一个人说了什么，容易变成 4 段独立回答，而不是历史人物对线。
- 更合理的是两阶段：

第一阶段并发轻量评估：

- 每个角色快速返回：
  - 发言意愿分数
  - 立场摘要
  - 想反驳谁/补充什么

第二阶段正式生成：

- 只选最高的 1-2 个角色。
- 生成主答和反驳。
- 这样比全员串行快，也比全员并发更像真实博弈。

优先级判断：

- 中低。
- 这是“群聊 2.0”体验升级，不是现在最急的技术债。
- 可以等主线内容和“入局”玩法更稳定后再做。

### 5. 多技术栈前端收拢

Gemini 建议：

- 项目同时有 Streamlit `app.py`、FastAPI StaticFiles 的网页端 `static/`、微信小程序 `miniprogram/`。
- 维护成本高。
- 建议将 Streamlit 剥离生产环境，仅作为内部剧本资产测试/Prompt 调试工具。
- 对外业务收拢为 FastAPI 后端 + 响应式 Web 单页 `static/` + 微信小程序。

当前判断：

- 基本同意。
- 生产主线应该是：
  - FastAPI 后端
  - `static/` 响应式网页
  - 未来如继续做，再维护 `miniprogram/`
- `app.py` 如果是早期 Streamlit 或测试入口，不要再作为生产主线。
- 可以保留为：
  - prompt 调试工具
  - 内容预览工具
  - 内部剧本测试工具
  - 快速试模型工具

优先级判断：

- 中。
- 不一定立刻删 `app.py`，但后续新增业务逻辑不要再往 Streamlit 里堆。

### 技术债总体优先级

当前建议排序：

1. AI 回复结构化输出 + Pydantic 校验。
2. 聊天记录从本地 JSON 迁移到数据库。
3. 生产架构收拢，`app.py` 降级为内部工具。
4. 小游戏 / 入局 / 穿越 session 入库。
5. 群聊并发 + 角色意愿打分。

总体评价：

- Gemini 的技术方向大多正确，但它站在“系统马上规模化”的角度，架构味偏重。
- 当前项目更应该按产品阶段推进：先修会导致用户报错、数据丢失、体验卡顿的地方；玩法型架构等需求稳定后再做。
- 下一次真正动代码，最推荐先做“结构化输出 + Pydantic 校验”，投入不大，稳定性收益明显。

## 产品路线 TODO

用户当前认可的优先级曾调整为：

1. 穿越 demo
2. 猜人物小游戏优化
3. 补充朝代内容
4. 邮件 / 登录系统
5. 视频

后来因为穿越过于复杂，暂时放一放，转向继续写厚历史事件内容。

当前最现实的下一步：

- 继续补其他朝代的关键事件。
- 继续把已有事件写厚。
- 优化猜人物小游戏。
- 等内容和玩法更稳定后，再考虑登录系统和视频宣传。

长期内容量设想：

- 约 11 个朝代。
- 平均每个朝代约 15 个事件。
- 总计约 165 个事件。
- 每个事件后续再逐步扩写。

## 后台统计

后台能看到：

- 访问次数
- 独立访客
- 事件浏览
- AI 对话
- 用户提问
- 猜人物互动
- 用户反馈

用户在看到统计后发现：加登录后访问很多但没人对话，因此调整为先体验后登录。

有登录系统后后台未来可改：

- 区分游客和登录用户。
- 统计注册转化。
- 统计登录后留存。
- 统计用户历史学习路径。
- 但不要展示完整 AI 回答和整段聊天记录，保持克制。

## 部署与环境变量

Render 线上目前有环境变量：

- `DATABASE_URL`
- `DASHSCOPE_API_KEY`
- `DEEPSEEK_API_KEY`
- `ROUTERLINK_API_KEY`
- `ADMIN_ANALYTICS_KEY`
- 曾经配置过 SMTP 相关变量和 `AUTH_SECRET`

不要把任何真实 key 写进代码、文档或最终回答。

如果后续使用阿里云邮件推送：

- 优先用 HTTP API / 官方 SDK。
- 不要依赖 raw SMTP 端口。
- 需要用户在阿里云控制台配置发信域名、AccessKey 或邮件推送相关密钥。

## 本地与浏览器测试注意

- Codex 可以操作 in-app browser，但有时浏览器可能被插件或状态影响，本地页面打不开时先检查服务是否运行。
- 用户很惊喜 Codex 能像真人一样操作浏览器，但不要过度强调。
- 有时 Chrome/无痕模式表现不同，尤其穿越 demo 的缓存和旧状态可能影响结果。
- 修改前端后，尽量本地打开 `http://127.0.0.1:8001/` 验证。

## 协作规则

新对话中的 Codex 应该：

1. 先读 `CODEX_HANDOFF.md`。
2. 再看 `git status --short`。
3. 如果用户让改 UI，优先改现有 `static/index.html`、`static/css/styles.css`、`static/js/app.js`。
4. 如果用户让改历史内容，优先改 `events.yaml`，并同步 `next_event`、导航标题、骨架节点引用。
5. 如果用户让推送 GitHub，再执行提交和推送；否则只做本地修改。
6. 解释时尽量说“我改了什么、你怎么本地看”，不要展开太多技术细节。
7. 内容写作要稳：正史骨架优先，现代分析其次，不要为了戏剧化乱编。

## 最近已完成事项速记

- 首页桌面和移动端背景图完成。
- 首页标题文字由代码渲染。
- 首页登录入口删除。
- 强制阅读前登录撤下，降低访问流失。
- 对话改为游客可体验 1 次，继续对话需要邮箱验证码登录。
- 阿里云 DirectMail API 已在线上跑通，QQ SMTP / raw SMTP 不再作为主方案。
- 后台已加每日注册用户统计。
- 汉朝 20 个主线事件完成并写厚。
- 汉朝支线“长乐宫血案”“巫蛊之祸”保留为支线。
- 汉朝导航顺序修正。
- 三国档案已扩充，并写厚现有事件；三国内容要注意正史边界。
- 汉朝 / 三国骨架标题和右侧描述错误已修正。
- 专家模式 `max_tokens` 改回 800。
- 快速模式从 `qwen-turbo` 改为默认 `qwen3.6-flash`，并关闭 thinking。
- 入局玩法已经改成“玩家作为最终决策者”，有 AI 先讨论、玩家追问、决定按钮和旁白推演。
- 入局事件卡、知识边界和分支推演规则已经搭起基础。
- 当前本地正在做电影感黑金 UI，顶部导航含“互动游戏”和“入局”，但这轮 UI 尚未提交。
- 右侧聊天滚动撑爆页面的问题已定位并修复：右侧内部滚动，不再带动中间卷宗。
- 秦朝部分偏硬的制度措辞已按用户反馈调整。
