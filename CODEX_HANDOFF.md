# 时空印证系统：Codex 交接说明

更新时间：2026-06-01
项目路径：`E:\gitclone\history-app2\history-app`
线上仓库：`https://github.com/wkq1991zmc/history-app`

这个文件给下一次新开的 Codex / AI 对话使用。新对话开始后，先读这个文件，再看 `git status --short`，基本就能接上当前项目状态。

## 当前最重要状态

- 用户没有编程背景，希望 AI 直接理解项目、直接改项目，用普通中文解释。
- 本地开发服务目前常用地址：`http://127.0.0.1:8002/`
- 当前重点页面：`http://127.0.0.1:8002/#time-travel-dev`
- 当前主要工作线是“入局玩法”的 UI、交互和历史考据感。
- Render 免费额度已用尽，线上暂时不可用；此前临时用过 Cloudflare Tunnel，但用户侧打不开，不要假设外网可访问。
- 当前有较多本地未提交改动，不要随意回滚。尤其不要还原 `api.py`、`static/index.html`、`static/css/styles.css`、`static/js/app.js` 和新增图片。

当前本地改动大致包括：

- `api.py`
- `static/index.html`
- `static/css/styles.css`
- `static/js/app.js`
- `static/images/ruju-entry-bg.png`
- `static/images/chibi-strategy-map.png`

除非用户明确说“提交 / 推送 GitHub / 部署”，不要提交或推送。

## 产品定位

项目名：时空印证系统。

一句话定位：这是一个中国历史学习网站。核心不是单纯讲故事，也不是只做 AI 聊天，而是帮助用户按朝代搭起中国历史的“骨架”：先看到主线节点，再通过事件故事、人物对话、朝代骨架页和“入局”玩法逐步补细节。

用户现在最看重：

- 历史主线的正史感和结构感。
- 让普通用户知道“有哪些事情值得问”。
- 通过人物对话理解历史处境。
- 通过“入局”进入历史决策现场，听取不同立场后亲自追问、拍板。
- 视觉上要有历史档案、黑金、暗红、古卷、史案系统的气质。

## 用户偏好

- 用户不懂代码，解释要通俗，不要堆技术术语。
- 用户通常希望 AI 直接做，不只是给方案。
- 用户很在意正史依据，尤其是《史记》《汉书》《后汉书》《三国志》《资治通鉴》等。
- 可以有现代分析，但要区分“史书记载”和“现代判断”。
- 用户不喜欢“国家机器”这个词，已经要求替换为更自然的同义表达。后续写内容时不要再用这个词。
- 视觉风格偏好：黑金、暗红、历史档案感、沉浸但不要太花。
- 不喜欢太模板化、太亮、太白、太营销页的设计。
- 用户目前不想公开“入局玩法”，但自己需要有入口能访问调试。

## 主要文件

- `api.py`：FastAPI 后端。事件接口、聊天接口、入局玩法、流式输出、登录、统计等都在这里。
- `events.yaml`：历史主线与事件内容库。
- `static/index.html`：前端页面结构。
- `static/css/styles.css`：主要样式。
- `static/js/app.js`：前端交互逻辑，包括导航、故事、聊天、入局、弹窗、流式输出等。
- `static/images/`：本地图片资源，包括入局背景和赤壁地图底图。

## 历史内容状态

已生成 / 已打磨：

- 秦朝主线。
- 汉朝主线。
- 三国主线。
- 晋朝主线。

用户说主线正在慢慢填充，目前是一个朝代一个朝代继续生成。后续还会继续让 AI 生成更多朝代。

最近做过：

- 修过左侧导航支线在网页上显示异常的问题。
- 生成了晋朝主线，要求严格遵循正史，参考秦、汉、三国的写法。
- 用户问过三国是否应该从黄巾起义开始，但后来明确说三国和汉朝先保持现状，不改。
- 用户觉得三国结尾“三分归晋”和晋朝开头前两篇重复。后来只微调了“三分归晋”和晋朝开头的衔接，不改汉朝和三国整体。
- 修过骨架页面文字颜色和背景太接近、看不清的问题。
- 替换 / 避免了“国家机器”这类用户不喜欢的表达。

内容原则：

- 主线故事要像历史档案，讲清“为什么重要、它改变了什么、它和下一步有什么关系”。
- 不要把《三国演义》的桥段当正史。
- 三国、晋朝交界处要避免重复：三国可以收束到司马氏完成统一趋势，晋朝开头则更应聚焦“统一之后的问题”。

## 入局玩法：产品理解

用户对入局玩法的对外介绍需求：

> 入局是一种历史决策现场体验：你不再只是读历史，而是坐到关键时刻的决策位，听见同一件事里不同人物的立场、恐惧和判断，再亲自追问、插入意见并最终拍板。系统会根据真实历史背景展开局势，让你在有限信息和复杂人心中做出选择。

用户后来希望把“插话”换成“说”，所以页面按钮现在应显示“说”。

当前入局定位：

- 入局暂时不公开给普通用户。
- 顶部“入局”可以进入，但主要给用户自己和演示用。
- 隐藏入口 / 开发入口：`#time-travel-dev`
- 默认进入赤壁战前江东朝议，不再让用户选择事件。

当前主剧本：

- 场景：东汉末年，公元 208 年，赤壁战前的江东朝议。
- 玩家身份：孙权，江东之主。
- 目标：保住江东基业，同时判断曹操是否真不可敌。
- 体验顺序大致是：
  - 点击随机入局 / 开始入局。
  - 黑屏加载，显示事件加载中和进度条。
  - 加载完成，点继续。
  - 黑屏显示前情提要，文字流式输出。
  - 点继续。
  - 显示“你的任务”，文字流式输出。
  - 点继续。
  - 进入赤壁战前江东朝议视觉场景。
  - 大臣按轮发言，所有发言流式输出。
  - 用户可以输入内容“说”，也可以“拍板”。

## 入局 UI 当前状态

入口页：

- 已按用户给的参考图方向做成黑金历史决策页。
- 背景图：`static/images/ruju-entry-bg.png`
- 页面文案精简为：
  - 入局
  - 进入真实历史决策现场，坐上决策位，听取众人意见后亲自落子。
  - 随机入局 / 开始入局按钮
- 事件选择已去掉，默认赤壁。
- 之前底部流程卡片有过“进入现场 / 收集观点 / 做出抉择”，用户后来要求精简，入口页现在应更简洁。

视觉对话页：

- 背景是江东朝议 / 江边楼阁场景。
- 背景应覆盖上方导航和左侧导航之外的整个右下内容区。
- 人物头像不是小圆头像，而是左侧较大的半身肖像。
- 肖像已做自适应，避免被拉长；缩放页面时应尽量不挡对话框。
- 对话框层级在头像上方。
- 对话框右上角有 `‹ / ›` 记录翻阅按钮：
  - 只翻已经出现过的对话记录。
  - 不推进下一句新对话。
  - 当前显示最新内容时，`›` 不显示或不可用。
  - 回看旧内容后，`›` 才出现，用来回到已经看过的新内容。
- “继续”按钮保持文字按钮，固定在对话框右下角。
  - 这是唯一推进下一句新对话的入口。
  - 不能被右上角的 `›` 替代。
  - 已给正文底部留空间，避免长文字和按钮重叠。
- 输入栏：
  - 文案：“以孙权身份发言、追问或下令...”
  - 按钮：“说”
  - 按钮：“拍板”
- “拍板”之前有 bug：输入框为空时点了没反应。现在已改为即使为空也会自动提交默认句：
  - `我意已决，按当前判断拍板执行。`

流式与等待状态：

- 大臣发言应一个字一个字输出。
- 文字未输出完时不能继续。
- 模型生成期间，对话框显示三个点动画，表示正在生成。
- 之前用户反馈“大臣回复只生成一个主字”“下一句没看清就跳过”，已经做过队列和等待修复。

## 入局地图弹窗

用户要求：

- 做一版赤壁战前地图。
- 使用“无文字底图 + 网页叠加考据标签”的模式。
- 能显示当时其他地区势力，避免被人说“不符合真实情况”。
- 在网页上加一个地图按钮，点击后弹窗显示地图。

已实现：

- 新增底图：`static/images/chibi-strategy-map.png`
- 底图是无文字古地图风格。
- 网页叠加标签包括：
  - 曹操北军
  - 江陵
  - 夏口 / 刘备、刘琦
  - 赤壁
  - 乌林
  - 柴桑 / 孙权
  - 益州刘璋
  - 汉中张鲁
  - 江东诸郡
  - 曹军南下
  - 孙刘水军线
- 弹窗有“势力关系”说明：
  - 曹操占据荆州后沿长江压向江东，江陵是重要后方。
  - 孙权据江东，决策与集结重心在柴桑一线。
  - 刘备、刘琦依夏口、江夏残部，与孙权合力拒曹。
  - 益州刘璋、汉中张鲁等仍为独立势力，未直接加入赤壁主战场。
- 弹窗中特意写了考据边界：
  - 示意战略关系，不作现代精确坐标。
  - 赤壁地望有争议。
  - 图中以“赤壁—乌林—夏口—柴桑—江陵”的史事关系呈现。
- 用户觉得地图太小，已放大：
  - 地图现在占弹窗主区域。
  - 势力说明移动到地图下方压缩展示。

注意：

- 这个地图是“战略关系示意图”，不是现代精确地理地图。
- 后续如果用户要求更严谨，应进一步查阅赤壁地望争议、乌林位置、柴桑与夏口关系，必要时加“地望争议”按钮或注释。

## 已参考的历史依据方向

赤壁战前局势主要参考：

- 《三国志·先主传》
- 《三国志·周瑜鲁肃吕蒙传》
- 《资治通鉴·卷六十五》

关键史事关系：

- 曹操平荆州后南下。
- 刘备败退并与刘琦势力依托夏口 / 江夏。
- 孙权在江东决策，鲁肃、周瑜主张抗曹，张昭等有降曹倾向。
- 孙权遣周瑜、程普等与刘备合力拒曹。
- 赤壁 / 乌林一线是孙刘与曹操水陆对峙的核心。
- 益州刘璋、汉中张鲁等是同时期其他地区势力，但不直接加入赤壁主战场。

## 当前 UI 细节和缓存版本

`static/index.html` 中 CSS / JS 版本参数经常更新，用于避免浏览器缓存。

最近版本包括：

- `ruju-map-modal-20260601`
- `ruju-dialog-nav-20260601`
- `ruju-map-large-20260601`
- `ruju-history-nav-20260601`
- `ruju-continue-corner-20260601`

当前最近一次用于“继续按钮右下角”的 CSS 版本：

- `styles.css?v=ruju-continue-corner-20260601`

JS 当前可能仍是：

- `app.js?v=ruju-history-nav-20260601`

如果改了 JS 或 CSS，记得同步更新版本参数，否则用户浏览器可能看到旧效果。

## 本地验证情况

最近用浏览器验证过：

- 入局页可打开。
- 前情提要和你的任务阶段可继续。
- 正式朝议页可进入。
- 地图按钮可打开弹窗。
- 地图弹窗底图正常加载，标签正常显示。
- 地图已放大。
- 对话右上角 `‹ / ›` 可以回看已出现内容。
- `›` 不会推进未出现的新内容。
- “继续”按钮是推进新对话的入口。
- “继续”按钮已移到对话框右下角，不再压在正文中间。
- “说”按钮文案已替换“插话”。
- “拍板”空输入也能生成默认拍板句。

控制台常见提示：

- Tailwind CDN 的生产环境警告，原本就有，不是本轮新增。

本机 `node --check static/js/app.js` 曾经失败：

- 报错是 WindowsApps 中 `node.exe` 权限拒绝。
- 这不是项目 JS 语法错误的直接证据。
- 后续主要通过浏览器实测确认。

## 部署与外网访问

Render：

- 用户截图显示 Render Hobby 免费带宽 5GB 用完，Workspace suspended。
- 当前 Render 暂时不可用。

替代方案：

- 曾尝试 Cloudflare Tunnel，生成了 `trycloudflare.com` 链接，但用户打不开，提示 `ERR_CONNECTION_RESET`。
- 之后继续以本地 `127.0.0.1:8002` 演示为主。
- 如果用户临时要给别人看，优先建议：
  - 用本机本地服务现场展示。
  - 或用同一局域网 IP。
  - 或临时换 Vercel / Railway / Fly.io / PythonAnywhere / paid Render，但要看项目后端依赖和环境变量。

## 登录与收费方向

用户之前的产品设想：

- 主线故事免费。
- AI 对话游客可先体验一次。
- 后续继续对话需要注册 / 登录。
- 入局玩法可作为未来付费重点。
- 现在暂不接支付。

登录：

- 邮箱验证码登录 / 注册是一套流程。
- 之前 Render SMTP 端口问题导致 SMTP 不稳定，已改为阿里云 DirectMail API。
- 不要把用户真实 AccessKey、邮箱、验证码等敏感信息写进代码或文档。

## 代码注意事项

- 用户可能随时在浏览器截图指出 UI 问题，优先按截图修。
- 修改文件用 `apply_patch`。
- 不要随便还原用户或前面 AI 的改动。
- 这个项目目前没有前端打包流程，主要是静态 HTML / CSS / JS + FastAPI。
- 前端大量逻辑在 `static/js/app.js`，样式大量集中在 `static/css/styles.css`。
- 该文件可能很长，搜索时用 `rg`。

## 新对话继续建议

新 AI 接手后建议顺序：

1. 先读 `CODEX_HANDOFF.md`。
2. 执行 `git status --short` 看未提交改动。
3. 如果用户继续说“入局页面”，直接打开 `http://127.0.0.1:8002/#time-travel-dev`。
4. 如果用户说“地图”，看 `static/images/chibi-strategy-map.png` 和 `#chibi-map-modal`。
5. 如果用户说“继续按钮 / 前进后退 / 拍板 / 说”，看 `static/index.html` 中 `travel-visual-panel`，以及 `static/js/app.js` 中：
   - `advanceVisualDialogue`
   - `showVisualHistoryIndex`
   - `browseVisualDialogueForward`
   - `submitVisualNovelInput`
   - `processVisualNovelPendingInput`
   - `updateVisualContinueAvailability`
6. 如果用户说“历史内容 / 主线 / 朝代”，看 `events.yaml`。

## 当前最可能的下一步需求

用户可能会继续要求：

- 微调入局对话框位置、头像大小、输入栏位置。
- 让地图按钮更好看或地图更严谨。
- 让“拍板”后的历史推演更像正式结局。
- 给赤壁场景增加更多人物互动。
- 改善移动端入局体验。
- 把本地当前效果想办法给别人看。
- 继续生成下一个朝代主线。

## 2026-06-01 GitHub 推送说明

用户要求把当前工作推送到 GitHub，并同步更新本交接文件。

本次准备推送内容：

- 入局入口页黑金历史决策风格。
- 赤壁战前江东朝议视觉对话页。
- 大臣发言、前情提要、任务说明的流式输出体验。
- 等待模型生成时的三点动画。
- 用户作为孙权发言、追问、拍板的交互。
- “插话”按钮改为“说”。
- “拍板”空输入时也能提交默认拍板句。
- 对话框右上角 `‹ / ›` 仅用于翻看已出现过的历史对话。
- “继续”按钮恢复为唯一推进新对话的入口，并固定到对话框右下角。
- 新增赤壁战前战略地图弹窗，采用无文字底图 + 网页叠加标签。
- 新增地图底图 `static/images/chibi-strategy-map.png`。
- 新增入局入口背景图 `static/images/ruju-entry-bg.png`。
- 本交接文件已重写为 2026-06-01 最新上下文。

推送后，新对话仍应先读本文件，再查看 `git status --short` 确认是否还有新改动。

## 对用户的沟通语气

用中文、自然、直接。

合适说法：

- “我来帮你改。”
- “这个问题我看到了，确实是位置不对。”
- “我会先保证不影响现有流程。”
- “这个地方为了避免被挑史实问题，我建议写成战略关系示意。”

不合适：

- 不要只讲一堆技术词。
- 不要让用户自己去改文件。
- 不要轻易说“这不可能”。
- 不要在没有验证时说“已经好了”。
